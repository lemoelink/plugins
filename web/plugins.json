"system_time.py": {
    "name_es": "Hora del Sistema",
    "name_en": "System Time Injector",
    "category": "context",
    "icon": "fa-solid fa-clock",
    "description_es": "Inyecta la fecha y hora local del servidor al inicio del chat para que el modelo sea consciente del tiempo actual.",
    "description_en": "Injects the server's local date and time at the start of the chat so the LLM is aware of the current time.",
    "features_es": [
      "Inyección silenciosa en segundo plano sin alterar el enrutamiento semántico.",
      "Formato de hora totalmente personalizable desde `config.json`.",
      "Evita que el modelo alucine con las fechas actuales."
    ],
    "features_en": [
      "Silent background injection that doesn't interfere with normal routing.",
      "Fully customizable date format via your root `config.json`.",
      "Prevents the LLM from hallucinating when asked about today's date."
    ],
    "setup_es": "1. Descarga `system_time.py` y mételo en la carpeta `plugins/`.\n2. Configura el formato en `config.json` bajo la sección `system_time` (opcional).\n3. Reinicia LeMoE.",
    "setup_en": "1. Download `system_time.py` and place it inside your `plugins/` folder.\n2. Adjust your preferred time format in `config.json` under `system_time` (optional).\n3. Restart LeMoE."
  },
  "user_profile.py": {
    "name_es": "Perfil de Usuario",
    "name_en": "User Profile Context",
    "category": "context",
    "icon": "fa-solid fa-user-gear",
    "description_es": "Carga la información y preferencias del usuario desde la configuración y las inyecta en el contexto para obtener respuestas adaptadas.",
    "description_en": "Loads the user's profile and tech preferences from config and injects them to get personalized answers.",
    "features_es": [
      "Carga automática de tu nombre, stack tecnológico e intereses.",
      "Permite que la IA te responda con un contexto y tono adaptado a ti.",
      "Inyección limpia e invisible de forma nativa."
    ],
    "features_en": [
      "Automatically loads your name, tech stack, and background preferences.",
      "Allows the active model to tailor responses to your specific profile.",
      "Silent and invisible integration out of the box."
    ],
    "setup_es": "1. Descarga `user_profile.py` y mételo en la carpeta `plugins/`.\n2. Configura tus datos en `config.json` bajo la sección `user_profile`.\n3. Reinicia LeMoE.",
    "setup_en": "1. Download `user_profile.py` and place it inside your `plugins/` folder.\n2. Setup your profile details in `config.json` under the `user_profile` block.\n3. Restart LeMoE."
  }
